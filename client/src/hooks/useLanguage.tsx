// Этот файл больше не нужен, так как мы используем LanguageProvider из компонентов
// Импортируем из основного файла вместо поддержки дублирующегося кода
import { useLanguage, Language } from '../components/LanguageProvider';

export { useLanguage, Language };